/**
 * Main entry point for stratiqai-types-simple
 * 
 * Exports:
 * - GraphQL schema
 * - GraphQL operations (queries, mutations, subscriptions)
 * - TypeScript types (generated by GraphQL Code Generator)
 * 
 * Note: TypeScript types are generated by running `npm run generate:types`
 * They will be available in './generated/types.js' after code generation.
 */

export * from './schema.js';
export * from './operations.js';

// Re-export generated types for convenience
// This ensures consumers get type-safe operations
export type * from './generated/types.js';

// Export mutation registry for dynamic resolution
export * from './graphql/mutations/registry.js';