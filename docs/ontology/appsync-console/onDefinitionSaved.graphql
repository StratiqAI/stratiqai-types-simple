subscription OnDefinitionSaved($projectId: ID!) {
  onDefinitionSaved(projectId: $projectId) {
    projectId
    id
    name
    description
    jsonSchema
    structuralHash
    properties {
      name
      description
      dataType
      path
      isList
      formula
      dependencies
    }
  }
}
